-- Initialize database with required extensions if needed

-- Create indexes for better performance
-- These will be created by Alembic migrations, but listed here for reference

-- Example: Create extension for UUID generation (if needed)
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Database is ready for application